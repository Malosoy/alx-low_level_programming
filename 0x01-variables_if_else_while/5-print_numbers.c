#include <stdio.h>

/**
* main - Entry point
*
* Return: Always 0 (Success)
*/
int main(void) 
{
	/**Print all single digit numbers of base 10 starting from 0*/
	for (int i = 0; i < 10; i++)
{
	printf("%d\n", i);
}

	return (0);
}
